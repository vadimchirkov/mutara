node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.*
/graft/.cache/

# Recipe tables are third-party game data — private, never committed.
data/
*.db
*.db-wal
*.db-shm
*.tgz
/AGENT-JOURNAL-HYPOTHESIS.md

# Live viewer artifacts, regenerated by trainers.
examples/pong/progress.json
examples/pong/champion.json

# Local compatibility link for historical journal paths.
/data

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
opencode.json
/examples/crypto-paper
